{
  "name": "techzakaz-max",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["backend", "bot", "frontend"],
  "scripts": {
    "build": "npm run build --workspace backend && npm run build --workspace bot && npm run build --workspace frontend",
    "test": "npm run test --workspace backend && npm run test --workspace bot && npm run test --workspace frontend",
    "test:integration": "npm run test:integration --workspace backend",
    "test:e2e": "playwright test",
    "lint": "npm run lint --workspace backend && npm run lint --workspace bot && npm run lint --workspace frontend",
    "audit:prod": "npm audit --omit=dev",
    "ai:start": "./scripts/start-local-qwen.sh",
    "ai:smoke": "node scripts/smoke-local-ai.mjs",
    "db:migrate": "npm run db:migrate --workspace backend",
    "db:seed": "npm run db:seed --workspace backend"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
